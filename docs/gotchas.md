# Gotchas — where the docs, the tools and PROMPT.md disagreed

Each entry: what we expected, what is actually true (with the source), what we did. Newest at the bottom.

| # | Expected (PROMPT.md or memory) | Actually true | What we did |
|---|---|---|---|
| 1 | Spec Kit installs `/speckit.constitution` (dot) as `.claude/commands/*.md` | v1.0.3 installs **skills** `.claude/skills/speckit-*/SKILL.md`, invoked as `/speckit-constitution` (hyphen, `invoke_separator: "-"` in `.specify/integration.json`). Also adds `/speckit-converge` and `/speckit-taskstoissues`. | Use hyphenated names everywhere; document the two extra commands as unused. |
| 2 | `specify init --no-git` exists | It does not in v1.0.3; `--here --force --non-interactive --integration claude --script sh` is the scripted form. | Used that form. |
| 3 | ingress-nginx + cert-manager for ingress | ingress-nginx was retired (repo archived 2026-03-24, no security fixes; kubernetes.io says do not deploy it). kind's own docs now point to Gateway API. | Traefik from the official chart as `HelmRelease/traefik`, plain `Ingress` kept. Confirmed by the user. |
| 4 | The hook `terminalSequence` field does not exist (a research subagent said so) | It exists: hooks reference, "Emit terminal notifications" (OSC 0/1/2/9/99/777 + BEL). The subagent summarized a page instead of reading it. | Used it in `notify.sh`. Lesson: verify field names against the source text, not a summary. |
| 5 | A hook `if: "Edit(deploy/**)"` on `matcher: Edit\|Write` covers Write calls too (permission rules treat `Edit(path)` as covering all file writes) | A hook `if` matches **only the tool it names**: with `Edit(...)` the hook never ran on a `Write` (probe on 2026-09-02, nothing in `hooks.log`); with `Write(...)` it did. | Two handlers per path prefix. For the formatter, dropped `if` and let the script switch on the extension. |
| 6 | New skills/agents are picked up live | Skills: yes, but only if the top-level `.claude/skills/` directory existed at session start; agents: same for `.claude/agents/`. Both were created mid-session here. | The Skill tool could not see `speckit-constitution`; the constitution was written by following the skill's steps by hand. Restart before the walkthrough. |
| 7 | `${CLAUDE_PROJECT_DIR}` expands in `.mcp.json` | Only with a default: `${CLAUDE_PROJECT_DIR:-.}` (the variable is set in the *server's* environment, not Claude Code's). | Used the default form for the Playwright output dir. |
| 8 | MCP Python SDK: `from mcp.server.fastmcp import FastMCP` | SDK v2 renamed it: `from mcp.server import MCPServer`; in-memory tests use `from mcp import Client`. | Feature 004 uses the v2 API. |
| 9 | Renovate `fileMatch`, `uv` manager | Key is `managerFilePatterns`; there is no `uv` manager, `pep621` handles `pyproject.toml` + `uv.lock`; `flux`/`kubernetes` managers need explicit patterns; `pre-commit` manager is off by default. | Feature 006 config follows the current names. |
| 10 | PROMPT.md §5.5 wants a `PreToolUse` deny hook for `kubectl delete` / `git push --force`, §2.4 says prefer `permissions.deny` | The hooks reference says the `if` filter is best-effort and "use the permission system rather than a hook to enforce a hard allow or deny". | Plain blocks → `permissions.deny`; the Bash hook guards the argument-dependent case (mutations outside `flux-system`). |
| 11 | One namespace `flagpole` | Two overlays in one cluster cannot share a namespace with identical resource names. | `flagpole-dev` and `flagpole-prod`. |
| 12 | CLAUDE.md < 150 lines (prompt) vs < 200 (docs) | Docs: "Keep CLAUDE.md under 200 lines". | Kept the stricter 150. |
| 13 | The `Stop` hook's `timeout` is a tool-event limit (≤ 10 s) | `Stop` is not a tool event; defaults are 600 s. Fast tests need more than 10 s once pytest starts. | `timeout: 120`, target `< 60 s`. |
| 14 | The gh token from `gh auth` works for `flux bootstrap github` | It is an OAuth token (`gho_`) with `repo` scope; Flux accepts any token with repo access via `GITHUB_TOKEN`. To be verified in Phase 4. | Will use `GITHUB_TOKEN=$(gh auth token)`; fall back to a fine-grained PAT if rejected. |
| 15 | Project `permissions.allow` rules apply as soon as `.claude/settings.json` exists | Headless `claude -p` printed: "Ignoring 31 permissions.allow entries from .claude/settings.json: this workspace has not been trusted. Run Claude Code interactively here once and accept the trust dialog". Deny rules still applied. | Documented in `docs/claude-code/settings.md`; the walkthrough's first interactive step is accepting trust. |
| 16 | `.claude/skills/` created mid-session is invisible until restart | True for the *Skill tool* immediately after `specify init`; about 10 minutes later the same session listed all `speckit-*` skills (live change detection caught up once the directory watcher registered). | Restart is the reliable path; document both observations. |
| 17 | Project subagents in `.claude/agents/` are usable as soon as the file exists | The Agent tool answered `Agent type 'code-reviewer' not found` right after the file was created; about 40 minutes later the same session announced the four agents as available (live discovery lags, like gotcha #6/#16). | Ran a general-purpose agent instructed to follow `code-reviewer.md` verbatim for the first review; later reviews use the real agent. |
| 18 | `/speckit-clarify` asks one question at a time | PROMPT.md §9 says batch questions; the clarify skill says exactly one per turn. | Batched the four clarifications in one `AskUserQuestion` (user instruction wins over tool procedure); recorded here. |
| 19 | `check-json` accepts any `.json` | `frontend/tsconfig.app.json` and `tsconfig.node.json` ship comments from the Vite template. TypeScript reads JSONC; Python's `json` does not: "Expecting property name enclosed in double quotes: line 9 column 5". | Excluded `^frontend/tsconfig.*\.json$` from `check-json` rather than stripping the template's comments. |
| 20 | gitleaks only fires on real secrets | A shell comment naming both feature branches after `Spec:` tripped `generic-api-key` (entropy 3.62): the colon reads as an assignment and the two branch names as a high-entropy value. | Reworded the comment to prose ("for specs 001-… and 002-…"). Prefer rewording over an allowlist entry, which would blind the rule to real keys. Two traps: the hook scans the **staged** copy, so `git add` before re-running, and quoting the flagged string verbatim in this table trips the same rule again. |
| 21 | Skill arguments are free text | A skill body using `$0` and `$1` splits the invocation on whitespace: `/e2e Run the 002 suite…` put `Run` in `$0` and `the` in `$1`, and the body rendered `make e2e TARGET=Run ARGS="the"`. | Use `$ARGUMENTS` unless the `argument-hint` really is positional, and keep the hint honest. Invoking a skill for real is the only way to catch this; the drift was invisible while the skill was only read. |
| 22 | "Chores commit on `main`" is easy to honour until a chore appears mid-feature | Three tooling fixes (`fix(dev)`, `fix(skill)`, `docs(gotchas)`) were committed on the `002-flagpole-web` branch because they were discovered while finishing it. The code reviewer caught the rule violation, not a hook. | Left in place rather than rewriting history, and noted here. The rule is about where chores *start*; a chore found mid-feature is cheaper to commit in place than to stash, and it reaches `main` with the merge either way. Worth a hook only if it recurs. |
| 23 | `tsc --noEmit` type-checks the project | Against a *solution* file (`"files": []` + `references`, which is what Vite scaffolds) it compiles nothing and exits 0. The lint gate looked green for the whole feature and could never have failed; `tests/` and `e2e/` were in no project at all. | `tsc -b --noEmit`, one project per source root, and prove the gate bites by planting an error in a file it is supposed to cover. |
| 24 | A Playwright `webServer` command always runs | With `reuseExistingServer: true` and a service already answering, the command is skipped entirely — so setup written into that command (rendering the Dex config) silently does not happen. A run on a different port then failed 8 of 9 tests against a Dex still holding the previous run's redirect URIs. | Put setup in the config module itself, where it runs on every invocation; leave `webServer.command` for starting the process. |
| 25 | `git checkout <file>` undoes a mutation-test edit | It restores the last **commit**, not the state before the edit. Uncommitted implementation work vanishes silently, and the suite then passes against the *old* code, which looks like a healthy result. Hit while mutation-checking the operator slot in 004. | Copy the file aside before mutating and restore from the copy; never use `git checkout` for a temporary edit on uncommitted work. |
| 26 | `@playwright/mcp --output-dir <path>` puts every artefact there | Screenshots honour it, but page snapshots and console logs landed in `.playwright-mcp/` at the repository root instead — untracked files in a clean tree, one of them a console log. Seen with `0.0.80` while verifying 004. | `.playwright-mcp/` added to `.gitignore` as well; check `git status` after a browser session rather than trusting the flag. |
| 27 | The GitOps guard hook inspects the `kubectl` command you are running | It inspects the whole Bash command string, so writing a *script* that contains `kubectl run` through a heredoc is denied exactly like running it — the hook cannot tell authoring from executing. Hit while writing `scripts/verify-cluster.sh`. | Write such files with the Write tool rather than a heredoc; and take the denial as a design question — the probe was rewritten to `--dry-run=server` and `kubectl exec` into a workload that already exists, so the script mutates nothing either way. |
| 28 | A virtual environment can be copied between paths in a multi-stage build | Its console scripts carry absolute shebangs, so a venv built at `/build/.venv` and copied to `/app/.venv` fails at start with `exec /app/.venv/bin/uvicorn: no such file or directory` — an error that reads like a missing file rather than a wrong interpreter path. | Build at the same path the runtime stage uses (`WORKDIR /app` in both), or invoke through `python -m`. |
| 29 | A key file mounted into a non-root container is readable | `chmod 600` on the host makes it readable by the host user's uid only; a container running as 10001 gets `PermissionError` on a file it can see. Hit smoke-testing the consumer image. | In Kubernetes give the Secret volume a `defaultMode` the running uid can read and set `fsGroup`; the private key stays unreadable to everyone else. |
| 30 | Flux retries a Kustomization until its resources become applicable | kustomize-controller dry-runs the **whole** set first, so one unknown kind aborts the apply — including the apply that would have installed the controller defining that kind. Putting cert-manager and a `ClusterIssuer` in one unit deadlocks: nothing is ever created, and the message (`no matches for kind "ClusterIssuer"`) reads like a missing CRD rather than a dependency loop. | Whenever a repository installs a controller *and* uses its custom resources, split them into two Kustomizations with `dependsOn`. |
| 31 | A third-party chart runs under the restricted Pod Security Standard | Most charts set no `securityContext` at all, so the namespace rejects the Pod and the Helm install hangs until it times out — the HelmRelease says `timeout waiting for Deployment` and the real reason is only in the ReplicaSet's events (`violates PodSecurity restricted:latest`). Hit with Dex 0.24.1. | Set `podSecurityContext` and `securityContext` in the chart's values to the same floor your own workloads meet, and read `kubectl -n <ns> get events` rather than the HelmRelease message when an install hangs. |
| 32 | A check that reads `.spec.template.spec.containers[*].env` sees the container's environment | Not when it arrives through `envFrom`: the values live in the ConfigMap, and the field is empty. The grant check failed for dev and — worse — **passed** for prod, because "absent" was what it expected and absent was all it could ever find. A check that cannot fail is not a check. | Resolve the `configMapRef` and read the ConfigMap; then flip the expectation in the contract and confirm the check turns red. |
| 33 | `sops --decrypt` finds the age key that encrypted the file | It looks in `SOPS_AGE_KEY_FILE`, `SOPS_AGE_KEY` and `~/.config/sops/age/keys.txt` — not at a key named anything else. Encrypting works without any of them (it needs only the public recipient), so the first failure appears at the first decrypt, long after the file was written. | Export `SOPS_AGE_KEY_FILE` (it is in `.env.example`) or name the key `keys.txt`. `make` targets source `.env`; an ad-hoc shell does not. |
| 34 | A browser reaches a cluster host served by the cluster's own CA | Chromium refuses with `ERR_CERT_AUTHORITY_INVALID`: the CA is self-signed and in nobody's trust store. The Playwright MCP server needs `--ignore-https-errors`, and a human needs the CA installed (which takes sudo). | `--ignore-https-errors` added to `.mcp.json`; `docs/walkthrough.md` prints the command to trust the CA for a human, and does not run it. |
| 35 | `renovate-config-validator` refuses a deprecated configuration key | It does not. Given `fileMatch` (renamed to `managerFilePatterns` in Renovate 41) it prints `WARN: Config migration necessary`, shows the migration diff, and **exits 0**. A CI step that reads the exit status alone passes a configuration whose managers may silently not match anything. | Grep the validator's output for `migration necessary` and `ERROR` rather than trusting `$?`, and assert the absent key in a contract check as well. |
| 36 | `set -e` is a reasonable default for a script that runs several checks | Not when the checks are independent. Eight scanners behind `set -e` stop at the first finding, so you get one run per fix instead of one fix per run — and the seven you have not seen yet are invisible when you estimate the work. | Run every check, collect the outcomes, fail once at the end with a summary. `set -uo pipefail`, never `-e`, for that shape of script. |
| 37 | A scanner that reports nothing has found nothing | `trivy config` accepts a single directory and exits FATAL on more than one. Given three it wrote an empty report, `jq` extracted nothing, and the summary said **clean** — a check that could never fail, found by this feature's own first run. | Treat output that does not parse as a failure, treat a missing tool as a failure, and never let 'no findings' and 'no run' produce the same result. |
| 38 | A secret scanner catches any planted secret when you test it | gitleaks allowlists well-known example values. `wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY` — the AWS documentation example — produces no finding, so a probe built from it says the scanner is broken when it is working correctly. | Probe with a realistically shaped, randomly generated value (`ghp_` + 36 random characters detects as `github-pat`). Then remove it. |
| 39 | Pushing a file under `.github/workflows/` needs no special permission | Over **HTTPS** it needs the `workflow` scope on the token, and `gh auth status` here reports only `gist, read:org, repo`. This repository's remote is SSH, so the push uses the key and the scope never applies — which means the trap is invisible until someone clones over HTTPS. | Check the remote's protocol before blaming the file. `gh auth refresh -s workflow` adds the scope for an HTTPS remote. |
| 40 | `trivy fs` and `trivy config` cover container images | Neither looks inside one. `fs` walks a directory and `config` reads manifests; the packages in a base image are invisible to both. Three digest-pinned bases here were carrying **28 HIGH/CRITICAL** CVEs that no scanner reported, while the contract file recorded trivy's coverage as "images and manifests". | Run `trivy image` explicitly over every base named in the Dockerfiles. Because they are pinned by digest, the same bytes are scanned locally and in CI without either having to build anything. |
| 41 | Scanning a copy of the tracked files is always safer than scanning the tree | `.gitignore` is itself a tracked file, so it travels with the copy — and osv-scanner honours it. Pointed at the copy it reported `No package sources found` and the summary still said clean. The same copy is exactly right for gitleaks and `trivy fs`, which is what made the failure look like success. | Name the lockfiles (`--lockfile a --lockfile b`) rather than handing osv-scanner a directory, and refuse to run if one is missing. |
| 42 | `[.. | .automerge? // empty] | all(. == false)` checks that nothing sets automerge | jq's `//` treats `false` as absent, so every `automerge: false` is dropped and the array is empty — and `all` on an empty array is `true`. The assertion passes on a configuration that never mentions automerge, and on one that sets it to `false`, and it cannot distinguish either from a correct config. A third can-never-fail check, in the checker written to catch them. | Ask the positive question — does any key of that name have the value `true`? — and pass when the answer is no. Then plant `automerge: true` and confirm it turns red. |
| 43 | `mise install` will fetch any pinned npm package | It refuses a **trust downgrade**: `npm:renovate@44.59.3` was rejected because the transitive `@yarnpkg/libzip@3.2.2` has no provenance attestation where 3.2.0 had one. The refusal is correct and should not be excluded away for convenience. | Pin the version at the call site instead (`npx --yes --package renovate@44.59.3 -- …`). A bare `--package renovate` executes whatever npm published that morning, which is the thing worth avoiding. |
| 44 | `claude plugin marketplace add .` adds the current directory | It is rejected — `✘ Invalid marketplace source format. Try: owner/repo, https://..., or ./path`. The bare `.` is not recognised as a path; `./` is. An absolute path works too. Two characters, and the error names three forms without saying which one `.` failed to be. | Use `claude plugin marketplace add ./`. The marketplace file itself must be at `.claude-plugin/marketplace.json` relative to that path. |
| 45 | Installing the plugin from the CLI is the same as enabling it for the project | `claude plugin install <name>@<marketplace>` reports `(scope: user)` — it writes to the user's settings, so it follows the person, not the repository. A teammate cloning the repository gets nothing from it. | Both, on purpose: `extraKnownMarketplaces` + `enabledPlugins` in the project's `.claude/settings.json` is what makes the plugin arrive for anyone who trusts the folder. The CLI install is for trying it, and for repositories you do not control. |
| 46 | Packaging components into a plugin is free | It costs a namespace and a per-session budget. Every component becomes `plugin:component`, so every cross-reference has to move with it — `allowed-tools`, the skills' prose, `CLAUDE.md`, the docs. And `claude plugin details <name>` prints the real number: five small components here add **~510 tokens to every session**, invoked or not. | Run `claude plugin details <name>` before deciding. For a single repository, `.claude/` costs nothing and needs no manifest, no marketplace and no namespace. |
| 47 | A placeholder in a template is inert | `case "$rel" in <protected-dir>/*)` does not parse — shellcheck reports SC1073 and the file is not a valid script, so a template that was never run ships broken. The angle brackets read as a redirection. | Put placeholders in a quoted variable (`GUARDED="protected-dir"`), and run the templates: copy them into an empty repository, feed the hooks a real event, and check the JSON and frontmatter parse. |
| 48 | Trusting a local CA with `update-ca-certificates` makes browsers accept it | It fills the **system** store, which serves `curl` and `openssl` — and nothing else. Chrome reads NSS at `~/.pki/nssdb`, Firefox keeps a store per profile, and a snap Firefox cannot even read `/tmp`. So `curl` returns 200 while the browser still refuses, and the two disagreeing looks like a cluster fault. Worse, Chrome loads NSS **at startup**: after a correct `certutil` import an already-open window keeps failing while every fresh process says the certificate is fine. | `docs/walkthrough.md` now gives all three stores, and verifies with `google-chrome --headless=new --dump-dom` rather than `curl` — the same binary and the same store as the browser being fixed. When a certificate looks untrusted in one place and trusted in another, ask which store answered before touching the certificate. |
| 49 | `groups:` under a Dex `staticPasswords` entry sets the user's groups | Only from Dex **2.45**. In 2.44.0 — which is what chart 0.24.1's appVersion pins — `cmd/dex/config.go` declares `type password storage.Password`, and that struct had no `Groups` field, so the non-strict YAML unmarshaller **accepted the key and discarded it**; `passwordDB.Login` then returned an identity with no groups. No error, no warning, and Dex logs nothing for an authorization request at INFO level, so the only visible symptom was every user signing in as a `viewer` with the whole UI disabled. | Read the claims, not the config: completing an authorization-code flow by hand showed an `id_token` carrying `email`/`name` and no `groups` at all, which turned a guess into a fact in one step. Fixed by overriding `image.tag`/`image.digest` above the chart's appVersion. |
| 50 | A green end-to-end suite means the deployed system works | `docker-compose.dev.yaml` has run Dex **2.45.1** since feature 002; the cluster took the chart default, **2.44.0** — straddling exactly the release where gotcha #49 was fixed. One config file, two versions, and `US1-2 an operator signs in and sees their identity and role` passed locally in every run while the cluster silently gave everyone the wrong role. The suite was not weak; it was pointed at the wrong system. | Pin the *same* version in both places, and treat any component the local stack and the cluster obtain differently as a version split waiting to happen. And note what is *not* here: `frontend/playwright.config.ts` hardcodes `baseURL: http://localhost:${WEB_PORT}`, and `make e2e` takes only `ARGS` — there is no cluster target, though the `/e2e` skill's closing note advertised one until this was found. A suite that can only ever run against localhost cannot see this class of bug, and the skill describing a target the Makefile does not implement is the same drift one layer up. |
| 51 | `make help` is too simple to break | It printed `Makefile` as the name of every target. `-include .env` adds a second entry to `MAKEFILE_LIST` the moment `.env` exists, `grep` over two files prefixes each line with its filename, and awk's `FS=":.*?## "` then takes `Makefile` as field 1. The target list was correct in a fresh clone and broke on the first `make bootstrap` — so it was always right when anyone checked and always wrong for anyone following the instructions. | `grep -hE`. The `-h` is the whole fix. Found by running the first command of a tutorial being written for beginners, which is the only time anyone runs `make help` on a configured checkout. |
| 52 | `scripts/check-image-pins.sh` catches an unpinned base image | It skipped the worst case. To tell a stage reference (`COPY --from=build`) from an image it asked `[[ "$image" == *[:/]* ]]`, so a bare `FROM alpine` — implicit `:latest`, the least pinned thing a Dockerfile can say — matched no colon and no slash and was silently treated as a stage name. The probe printed in the script's own header, `printf 'FROM alpine\n' > /tmp/Dockerfile.probe && scripts/check-image-pins.sh /tmp/Dockerfile.probe`, therefore exited **0**: a documented proof that proved nothing. | Collect the stage names the file actually declares (`FROM x AS name`) and treat a bare token as a stage only if it is one of them. Punctuation cannot answer that question. Now the header's probe exits 1, and `alpine:3.20` is caught too. Same family as #37 and #42. |
| 53 | An `upload-artifact` step that goes green uploaded something | It uploaded nothing, for weeks. `.scan/` is a dot-directory and v4+ defaults to `include-hidden-files: false`, so both globs were filtered out before upload; `if-no-files-found` defaults to `warn`, so the step stayed green and said only `No files were found with the provided path`. Two defaults conspiring, neither of them written anywhere in the workflow file. The scan itself was genuinely running — all eight scanners reported real states — so what was lost was the evidence trail, not the scanning. | `include-hidden-files: true` and `if-no-files-found: error`. Both are now named in `ci-contract.json` and asserted, because the bug was a *default*: nothing in the file changed, and nothing in the file could have shown it. Proved by deleting each key and watching the assertion turn red. |
| 54 | `flux get kustomizations -A` column 4 is READY | It is SUSPENDED; READY is column 5. `session-start.sh` printed `$4`, so every session for months opened with `flagpole-dev=False flagpole-prod=False flux-system=False platform=False` against a cluster where all five were **Ready** — and a genuinely suspended kustomization, the one state worth waking up to, would have read `True`. Inverted for the only signal the line exists to carry. Nothing caught it because the hook's test runs in a temp directory with no cluster, so the branch never executed. | `$5`. The test now stubs `k3d` and `flux` on `PATH` with the real tab-separated column layout and asserts a Ready unit reads `True` and a suspended one reads `False`, so the branch runs in CI. A probe that cannot reach the code it probes is not a test. |
| 55 | One tool pinned once is one version | Node is pinned three times — `.mise.toml`, the `setup-node` step in `ci.yml`, and the digest in `frontend/Dockerfile` — and they had drifted: tests run on **24.18.0**, the shipped image is **24.20.0**. The comment in `.mise.toml` claimed it was "the major frontend/Dockerfile builds with, pinned to the patch", which was simply untrue. Renovate made it structural: the first two are in the `tooling` group and the third in `container images`, so they were never going to move together. Exactly the mechanism of #50, where the same split across Dex versions shipped a broken role to every user. | Assert what can be asserted offline — `check-ci-contract.sh` now fails if `.mise.toml` and `ci.yml` disagree — and *name* what cannot: comparing the image's patch needs the digest resolved, so the contract records it as unchecked instead of implying all three agree. A `node runtime` Renovate group tries to keep them together, though whether it matches is decided server-side. The versions were left alone: changing a shipped runtime on the strength of a test run made under load average 31 would be the opposite of this repository's point. |
| 56 | "Lands in feature 002/003" is a promise someone keeps | `specs/001-flagpole-api/quickstart.md` told the reader to get tokens from `scripts/dev-token.sh`, "in feature 002/003". Both features shipped and merged; the script never existed. Five of that page's seven `curl` scenarios need a bearer token, and the only other token source here is a **pytest fixture** — not callable from a shell — so the stated fallback was hollow too. A forward reference is invisible to every check: the path is missing *on purpose*, so nothing can assert it, and when the named feature merges there is no step that says "go back and look". | Wrote the script (authorization-code + PKCE against local Dex, the same exchange the browser performs) and replaced the promise with commands and their observed output. The general defence is to keep runnable instructions in a document something asserts — `scripts/check-tutorial.sh` would have caught this on day one. |
| 57 | `pkill -f` from the host only reaches host processes | k3d runs the cluster's containers in the host PID namespace, so the pods appear in `ps` with **the same command line** as the local dev server. Cleaning up a stray `uvicorn app.main:create_app` with `pkill -f` matched six processes owned by uid 10001 under `/app/.venv` — the running `flagpole-api` pods in `flagpole-dev` and `flagpole-prod`. Only a sandbox refusal (`Operation not permitted`) stopped it from taking down the cluster this repository had just finished verifying. | Never `pkill -f` a pattern that a container might also match. Kill the PID you started, or stop the service the way it was started. This is the process-table twin of the `kubectl` guard: the local stack and the cluster look identical from the outside, and only one of them is yours to restart. |
| 58 | A lint rule fires on the code that breaks it | `oxlint`'s `react(set-state-in-effect)` flagged `void loadFlags()` in an effect and said nothing about `void loadAudit()` on the very next line — the same pattern, the same synchronous `setState` before the first `await`. The discriminator is not the code: a two-callback reproduction shows the rule follows **zero-argument** callbacks only, and `loadAudit` takes `(before?: number)`. Contorting the flagged one to satisfy the rule would have left its identical twin untouched. | Kept the pattern (the `"loading"` transition is required — returning to a `"ready"` view would show stale data with no indicator, and on mount React bails out because the value is unchanged), suppressed the rule on that one line with the reasoning written above it, and recorded the analysis limit here. Also worth knowing: `oxlint-disable-next-line` applies to the literal next line, so a directive followed by an explanatory comment block disables nothing. |
| 59 | Unit tests are hermetic | They read the developer's environment. `Settings` is pydantic-settings, so every field not passed explicitly falls back to `os.environ`; `.env.example` opens with "Copy to .env"; and the Makefile does `-include .env` + `export`. So `make test` ran the suite with whoever's configuration was on the machine. The victim was the fixture that exists to prove an **unconfigured** operator grant refuses writes: it deliberately leaves those fields unset, and the environment filled them in, so the app under test was granted after all — `DID NOT RAISE`, and two errors. It appeared the day `make bootstrap` began creating `.env`, and it was always one copied file away for anyone who followed `.env.example`'s own first line. | A session-scoped autouse fixture in all three suites strips `FLAGPOLE_*` from `os.environ` and restores it afterwards. Proved by re-running the exact two variables that broke it: 1 failed + 2 errors before, 12 passed after. A test whose result depends on the machine it runs on is not a test of the code. |
