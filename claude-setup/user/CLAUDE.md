# Personal preferences (user CLAUDE.md example: ~/.claude/CLAUDE.md)

<!-- Applies to every project on this machine. Keep it about YOU, not about any project. -->

- Answer briefly; show the command before running it.
- I use tmux and Neovim; don't suggest IDE-specific steps.
- Default to `uv` for Python and `npm` for Node unless the repo says otherwise.
