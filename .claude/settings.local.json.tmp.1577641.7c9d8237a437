{
  "permissions": {
    "allow": [
      "Bash(python3 -)",
      "Bash(git add *)",
      "Bash(git commit -q -m 'fix\\(platform\\): split the certificate authority into its own reconciliation unit *)",
      "Bash(git commit -q -m 'fix\\(traefik\\): chart 41.x moved redirections, TLS and logging *)",
      "Bash(flux reconcile *)",
      "Bash(git commit -q -m 'chore\\(platform\\): shorten the health-check window to 5m *)",
      "Bash(git commit -q -m 'fix\\(dex\\): give the chart a security context the namespace will admit *)",
      "Bash(git commit -q -m 'fix\\(ingress\\): strip /api before the flag service sees it *)",
      "Bash(flux get *)"
    ]
  }
}
