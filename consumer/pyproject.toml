[project]
name = "flagpole-consumer"
version = "0.1.0"
description = "Flagpole consumer: one page showing what a flag does for a user. Spec: 003-flagpole-consumer."
requires-python = ">=3.14,<3.15"
dependencies = [
  "fastapi>=0.141,<1",
  "uvicorn[standard]>=0.34",
  "jinja2>=3.1,<4",
  "httpx>=0.28",
  "pydantic>=2.13,<3",
  "pydantic-settings>=2.6,<3",
  "pyjwt[crypto]>=2.13,<3",
  "prometheus-fastapi-instrumentator>=8.1,<9",
]

[dependency-groups]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.25",
  "ruff>=0.16",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["B008"]  # Depends() in defaults is the FastAPI idiom

[tool.ruff.lint.isort]
known-first-party = ["app", "tests"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
filterwarnings = ["error::DeprecationWarning:app"]
