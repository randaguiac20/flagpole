apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  clusterIP: None      # headless: the StatefulSet's one pod is addressed directly
  selector: {app: postgres}
  ports:
    - {name: postgres, port: 5432, targetPort: postgres}
